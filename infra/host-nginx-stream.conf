# OBSOLETE — do not install.
#
# Earlier SNI mux (nginx stream / HAProxy) in front of MTProto broke FakeTLS
# (MSS clamp / ClientHello). Live setup uses Teleproxy directly on :443 with
# site fallback to docker nginx on 127.0.0.1:8444.
#
# See infra/teleproxy/README.md
